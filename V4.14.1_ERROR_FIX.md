# Zento v4.14.1 — Story runtime error fix

- Fixed `Assignment to constant variable` caused by reassigning `originalOpenConv`.
- Fixed story top bar scope errors (`openStoryComposer is not defined`, `loadStories is not defined`) by using explicit story API hooks.
- Bumped the frontend cache version from `app.js?v=100` to `app.js?v=101` in `index.html` and `sw.js` so Railway/browser service-worker caches load the fixed bundle.
- `public/app.js` passes `node --check`.
