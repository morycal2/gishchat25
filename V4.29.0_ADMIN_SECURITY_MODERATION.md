# Zento v4.28.0 — Advanced Creator/Admin Center

## مهم‌ترین تغییرات
- رفع مسیر مدیریت کاربران که در بعضی دیتابیس‌ها به دلیل ستون `is_bot` با 502 می‌شد.
- پنل سازنده با اسکرول داخلی، حالت روز/شب و تب‌های پیشرفته.
- مدیریت ادمین‌های سایت با نقش و permissionهای مستقل.
- گزارش فعالیت ادمین‌ها در `admin_audit_logs`.
- جستجوی کاربران با نام، username، ایمیل و UID نمایشی ۱۰ رقمی.
- محدود کردن کاربر از ۳۰ دقیقه تا ۱ سال و رفع/تغییر محدودیت.
- گزارش پروفایل و گزارش پیام؛ گزارش‌ها در پنل مدیریت.
- پشتیبانی زنده: کاربر و ادمین تا زمان زدن «پایان مکالمه» می‌توانند پیام بدهند.
- پاسخ مستقیم ادمین به گفتگوهای پشتیبانی.
- بستن مکالمه توسط کاربر یا ادمین.

## نکته UID
برای سازگاری با دیتابیس‌های فعلی، UID نمایشی از شناسه عددی کاربر با صفرهای ابتدایی تا ۱۰ رقم ساخته می‌شود؛ جستجو هم این مقدار را پشتیبانی می‌کند.

## Deploy
نسخه را روی Railway Deploy کنید و سپس `Ctrl + Shift + R` بزنید تا `app.js?v=108` لود شود. اسکیما در Startup نیز به‌صورت خودکار ensure می‌شود.

# Zento v4.29.0
- Professional custom admin dialogs; no browser prompt/confirm for admin moderation flows.
- Independent admin account creation with email/password and permission presets.
- Persistent `admin` service account is created automatically; set `ADMIN_DEFAULT_PASSWORD` on Railway to choose its login password.
- Warning records and admin notices with push delivery.
- Timed bans expose exact expiry and live countdown to the user; message sending and call initiation are blocked while banned.
- Report center supports warning and timed restriction actions.
- Support threads support warning/restriction actions.
- Profile report button is always visible beside block.
